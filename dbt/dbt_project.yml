name: ecommerce_analytics
version: 1.0.0
config-version: 2
profile: api_bi_profile

model-paths: ["models"]

packages-install-path: /tmp/dbt_packages

models:
  ecommerce_analytics:

    staging:
      +materialized: view
      +schema: staging

    intermediate:
      +materialized: view
      +schema: intermediate

    marts:
      +materialized: table
      +schema: marts